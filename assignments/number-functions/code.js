const add = (a, b) => {
  return 
}
const subtract = (a, b) => {
  return 
}
const multiply = (a, b) => {
  return 
}
const divide = (a, b) => {
  return
}
const mod = (a, b) => {
  return 
}
const averageOfTwo = (a, b) => {
  return 
}
const averageOfThree = (a, b, c) => {
  return 
}
const distance = (a, b) => {
  return 
}

const manhattanDistance = (x1, y1, x2, y2) => distance(x1, x2) + distance(y1, y2)

const euclideanDistance = (x1,y1,x2,y2) => Math.sqrt (distance(x1,x2)**2 + (distance(y1,y2)**2))
